# メンテナ向けガイド

このリストの価値は掲載数ではなく、**載っているものが正しいこと** です。
迷ったら載せない方を選んでください。誤掲載1件でリスト全体の信用が落ちます。

## 日々やること

自動化されていないのは **判断だけ** です。

| 来るもの | やること |
| --- | --- |
| `report` ラベルの PR | 中身を見てマージ or クローズ |
| `removal` ラベルの Issue | 下調べのコメントを読んで `/delist` するか判断 |
| `refresh` の PR | 改名・凍結の追跡結果。基本はそのままマージ |
| `misc` ラベルの Issue | 提案・不具合。手で対応 |

Issue のクローズは自動です。PR をマージしても却下しても、元の Issue は結果コメント付きで閉じられます。

## 報告 PR のレビュー

PR が立った時点で、機械は次を確かめ終わっています。**もう一度やる必要はありません。**

- 証拠ツイートが全件、対象アカウント本人の投稿であること
- 数値ID とユーザー名の対応
- 鍵アカウントでないこと
- 形式・カテゴリ名の妥当性
- 報告者が確認欄すべてにチェックしたこと

人が見るのはここだけです。

1. **カテゴリが実際に当てはまるか** — [POLICY.md](POLICY.md) の定義と証拠を突き合わせる。
   「気に入らない」を `ai-slop` に押し込んでいないか。
2. **私怨・意見の不一致ではないか** — 論争の相手を消しにきていないか。
   `note` が反論になっていたら赤信号です。
3. **個人情報・人格攻撃が混じっていないか** — `note` は行為の説明のみ。
4. **単発の言い間違いを咎めていないか** — 証拠は1件でも構いませんが、
   その1件が「誰にでもある失敗」なら見送ります。

問題なければマージ。`dist/` は push 後に自動で再生成されます。

### 却下する

PR を close するだけです。理由を PR にコメントしてから閉じてください。
そのコメントが報告者に届く唯一の説明になります。

判断に困ったら、閉じずに Issue で報告者に聞いてください。
本文を編集すれば自動で再検証され、PR も更新されます。

## 掲載解除

`removal` の Issue には、自動で下調べのコメントが付きます
（該当エントリの有無、証拠が今も生きているか、投稿者が本当にそのIDか）。

解除すると決めたら、その Issue にコメントします。

```text
/delist @example 人違いのため
```

対象は `@handle` / プロフィールURL / 数値ID のどれでも通ります。
書き込み権限のある人以外が使っても弾かれます。

エントリは **削除せず** `status: delisted` になります。
消すと「なぜ解除したか」が追えず、同じ報告が繰り返されるためです。
`dist/` からは除外されます。

解除の判断基準は [POLICY.md](POLICY.md) の「異議申立て・掲載解除」を見てください。
**本人確認は求めません。** 申請内容そのものを検証します。

## 定期実行

| いつ | 何が起きるか |
| --- | --- |
| 毎日 03:17 JST | `refresh` が改名・凍結・証拠の削除を検知して PR を作る |
| main への push | `dist/` を再生成してコミット |
| 毎週月曜 | 30日動きのない `report` Issue を閉じる（PR があるものは対象外） |

`refresh` の PR は原則そのままマージで構いませんが、
`status: suspended` が付いたものは、凍結ではなく単に取得に失敗しただけのことがあります。
連続して同じアカウントに付くようなら手で確認してください。

## カテゴリを追加する

カテゴリは4か所に書かれています。**すべて更新しないと CI が落ちます**（それが目的です）。

1. `scripts/lib/validate-core.mjs` の `CATEGORIES`
2. `schema/account.schema.json` の `properties.categories.items.enum`
3. `POLICY.md` の掲載カテゴリ表（`| \`名前\` | 定義 |` の行）
4. `.github/ISSUE_TEMPLATE/1-report.yml` の `options`

```bash
node scripts/selftest.mjs   # 4か所の一致を検査
```

定義は **証拠を見て第三者が同じ判断に至れる** 粒度で書いてください。
「不快」「低品質」のように主観でしか判定できないものは、あとで必ず揉めます。

## 手元で動かす

```bash
node scripts/selftest.mjs              # 定義の整合性チェック
node scripts/validate.mjs              # 形式チェック（オフライン）
node scripts/validate.mjs --network    # 証拠の投稿者IDまで照合
node scripts/build.mjs                 # dist/ を生成
node scripts/refresh.mjs               # 改名・凍結を追跡
```

報告の変換を手元で試す場合:

```bash
gh issue view 123 --json body --jq .body > body.md
ISSUE_BODY="$(cat body.md)" node scripts/issue-to-json.mjs
```

## 詰まったとき

**PR にチェックが走らない / `action_required` で止まる**
Actions の承認ポリシーです。Settings → Actions → General から緩めるか、
`gh api --method POST repos/OWNER/REPO/actions/runs/<id>/approve` で個別に承認します。

**報告の自動検証が失敗し続ける**
理由は Issue にコメントされています。X 側の一時エラーなら、本文を編集し直せば再実行されます。
証拠が削除済みの場合は自動では通りません。魚拓URLを添えた手動 PR で対応します。

**`dist/` が食い違う**
`accounts/` から自動生成されるものです。手で編集した PR は CI が拒否します。
`node scripts/build.mjs` を実行して差分を確認してください。
