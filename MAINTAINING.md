# メンテナ向けガイド

報告が1件届いたとき、メンテナに残る作業は驚くほど少なくなっています。
数値IDの解決も、証拠ツイートの投稿者照合も、形式の検査も、PRが立った時点で終わっているからです。

残るのは、機械に決められないことだけです。

## 日々やること

| 来るもの | やること |
| --- | --- |
| `report` ラベルのPR | 中身を見て、マージするか閉じる |
| `removal` ラベルのIssue | 下調べのコメントを読み、`/delist` するか判断する |
| `refresh` のPR | 改名や凍結の追跡結果。ほぼそのままマージ |
| `misc` ラベルのIssue | 提案や不具合。手で対応 |

Issueを閉じる操作は要りません。
PRをマージしても却下しても、元のIssueは結果のコメント付きで自動的に閉じます。

## 報告PRのレビュー

PRが立った時点で、次は確認済みです。

- 証拠ツイートが全件、対象アカウント本人の投稿であること
- 数値IDとユーザー名の対応
- 鍵アカウントでないこと
- 形式とカテゴリ名の妥当性
- 報告者が確認欄すべてにチェックしたこと

人が見るのは4点です。

1. カテゴリが実際に当てはまるか。
   [POLICY.md](POLICY.md) の定義と証拠を突き合わせます。
   気に入らない投稿を `ai-slop` に押し込んでいないか。
2. 私怨や意見の不一致ではないか。
   論争の相手を消しにきていないか。
   `note` が反論の体裁になっていたら赤信号です。
3. 個人情報や人格攻撃が混じっていないか。
   `note` に書けるのは行為の説明だけです。
4. 単発の言い間違いを咎めていないか。
   証拠が1件でも構いませんが、その1件が誰にでもある失敗なら見送ります。

問題がなければマージします。
`dist/` は push 後に自動で再生成されるので、手を入れる必要はありません。

### 却下の伝え方

PRを閉じるだけです。
ただし、理由をPRにコメントしてから閉じてください。
そのコメントが、報告者に届く唯一の説明になります。

判断がつかないうちは閉じずに、Issueで報告者に尋ねるほうが早く片付きます。
報告者が本文を編集すれば自動で再検証され、PRもそのまま更新されます。

## 掲載解除

`removal` のIssueには、自動で下調べのコメントが付きます。
該当エントリの有無、証拠が今も生きているか、投稿者が本当にそのIDなのか。
この3点までが機械の仕事で、解除するかどうかは人が決めます。

解除すると決めたら、そのIssueにコメントします。

```text
/delist @example 人違いのため
```

対象は `@handle`、プロフィールURL、数値IDのどれでも通ります。
書き込み権限のない人が使っても弾かれます。

エントリは削除せず `status: delisted` になります。
消してしまうと解除の経緯が追えず、同じ報告が戻ってきたときに同じ議論をやり直すことになるからです。
配布ファイルからは除外されます。

解除の基準は [POLICY.md](POLICY.md) の「異議申立てと掲載解除」にあります。
本人確認は求めません。
申請してきた人が誰かではなく、申請の内容そのものを検証します。

## 定期実行

| いつ | 何が起きるか |
| --- | --- |
| 毎日 03:17 JST | `refresh` が改名や凍結、証拠の削除を検知してPRを作る |
| main への push | `dist/` を再生成してコミットする |
| 毎週月曜 | 30日動きのない `report` Issueを閉じる（PRがあるものは対象外） |

`refresh` のPRは、原則そのままマージして構いません。
ただし `status: suspended` が付いたものは、凍結ではなく取得に一度失敗しただけのことがあります。
同じアカウントに連続して付くようなら、手で確認してください。

## カテゴリの追加

カテゴリの一覧は5か所に書かれています。
すべて更新しないとCIが落ちます。
落ちるようにしてあります。

1. `scripts/lib/validate-core.mjs` の `CATEGORIES`
2. `schema/account.schema.json` の `properties.categories.items.enum`
3. `POLICY.md` の掲載カテゴリ表
4. `README.md` のカテゴリ表
5. `.github/ISSUE_TEMPLATE/1-report.yml` の `options`

```bash
node scripts/selftest.mjs
```

定義は、証拠を見た第三者が同じ判断に至れる粒度で書いてください。
「不快」「低品質」のように主観でしか判定できない定義は、必ずあとで揉めます。

## 手元で動かす

```bash
node scripts/selftest.mjs              # 定義の整合性チェック
node scripts/validate.mjs              # 形式チェック（オフライン）
node scripts/validate.mjs --network    # 証拠の投稿者IDまで照合
node scripts/build.mjs                 # dist/ を生成
node scripts/refresh.mjs               # 改名と凍結を追跡
```

報告の変換を手元で試すこともできます。

```bash
gh issue view 123 --json body --jq .body > body.md
ISSUE_BODY="$(cat body.md)" node scripts/issue-to-json.mjs
```

## 詰まったとき

### PRにチェックが走らない、または `action_required` で止まる

Actions の承認ポリシーによるものです。
Settings の Actions から緩めるか、`gh api --method POST repos/OWNER/REPO/actions/runs/<id>/approve` で個別に承認します。

### 報告の自動検証が失敗し続ける

理由はIssueにコメントされています。
X側の一時的なエラーなら、報告者が本文を編集し直せば再実行されます。
証拠が削除済みの場合は自動では通らないので、魚拓URLを添えた手動PRで対応します。

### `dist/` が `accounts/` と食い違う

`dist/` は自動生成物なので、手で編集したPRはCIが拒否します。
`node scripts/build.mjs` を実行して差分を確認してください。
